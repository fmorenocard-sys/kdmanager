# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment & secrets — never commit
.env
.env.*
!.env.example
functions/.env*
functions/serviceAccountKey.json
functions/service-account.json
functions/test-discord-token.js
*serviceAccount*.json
# Pilot service-account keys (any Firebase project key placed in functions/)
functions/kd-41-manager.json
functions/kd-*-manager.json
functions/*-firebase-adminsdk-*.json

# User data & log dumps — may contain personal data or tokens
users.json
functions/all_users*.json
functions/output.*
/logs*.txt
/logs*.json
/latest_logs.txt

# Firebase deploy cache & emulator debug logs
.firebase/
firestore-debug.log
database-debug.log
ui-debug.log
pubsub-debug.log

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Agent scratch: one-off scripts, dumps and captured output
scratch/
tmp/
/*.txt
/*.cjs
/temp_player.json
/test-firestore.js
functions/dumpUsers*.js
functions/fix_name.js
functions/testAdminDb.js
scripts/fix_name.js

# BUG-008 : token de test émulateur (généré, non versionné)
tests/.testlogin.json

# Artefacts d'outil de design (export .dc.html + runtime généré dc-runtime) — pas
# du code app. Les VRAIES références de design (images/captures) restent versionnées.
design_references/*.dc.html
design_references/support.js

# Classeurs bruts de 2997 (bank ledger, deadweight nominatif, scans SoC) : ils
# etaient servis PUBLIQUEMENT par le Hosting de toutes les instances (SEC-001).
# Sortis du depot le 2026-08-28 ; conserves en local dans scratch/legacy-public-data/.
# Rien ne les lit au runtime. Ne pas les reintroduire ici.
public/data/
